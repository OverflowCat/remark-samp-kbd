# ``remark-samp-kbd`` Example

This is
`code1`,````` `code2` `````,
````` code`3 `````, ````` code`4 `````.

And we have ``sample code``, keyboard shortcut ```Ctrl```+```Shift```+```C```,
and variable like ````x````!